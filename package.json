{
  "name": "@tokenring-ai/aws",
  "description": "Integration for AWS services, focusing on S3 storage and STS authentication.",
  "version": "0.2.0",
  "dependencies": {
    "@tokenring-ai/agent": "workspace:*",
    "@tokenring-ai/app": "workspace:*",
    "@tokenring-ai/chat": "workspace:*",
    "@aws-sdk/client-s3": "^3.1025.0",
    "@aws-sdk/client-sts": "^3.1025.0",
    "@tokenring-ai/utility": "workspace:*",
    "zod": "^4.3.6"
  },
  "exports": {
    ".": "./index.ts",
    "./*": "./*.ts"
  },
  "license": "MIT",
  "scripts": {
    "eslint": "eslint --fix ./",
    "test": "vitest run",
    "build": "tsc --noEmit",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "type": "module",
  "devDependencies": {
    "vitest": "^4.1.1",
    "typescript": "^6.0.2"
  }
}
